{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://thefunkybits.github.io/rgm-publication/spec/catalog-v2/signatures.schema.json",
  "title": "RGM catalog detached signatures",
  "type": "object",
  "additionalProperties": false,
  "required": ["schemaVersion", "catalogId", "manifestSha256", "signatures"],
  "properties": {
    "schemaVersion": { "type": "integer", "const": 1 },
    "catalogId": { "type": "string", "pattern": "^[a-z0-9][a-z0-9-]*$" },
    "manifestSha256": { "type": "string", "pattern": "^[0-9a-f]{64}$" },
    "signatures": {
      "type": "array",
      "minItems": 1,
      "maxItems": 4,
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": ["algorithm", "keyId", "value"],
        "properties": {
          "algorithm": { "const": "ed25519" },
          "keyId": { "type": "string", "pattern": "^[a-z0-9][a-z0-9._-]{0,63}$" },
          "value": { "type": "string", "pattern": "^[A-Za-z0-9_-]{86}$" }
        }
      }
    }
  }
}
